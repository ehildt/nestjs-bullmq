---
"@ehildt/nestjs-bullmq": patch
---

Bump devDependencies and fix CI workflows: add missing `actions: read` permission and `actions/download-artifact` step in release CI; add custom token to automated dependency update PR; update pre-push hooks to include `test:cov` and `depcheck` checks.
